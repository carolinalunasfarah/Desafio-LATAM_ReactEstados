const SocialButton = ({ fbIcon, ghIcon, liIcon}) => {
  return (
   <>
      <i class= {fbIcon}></i>
      <i class= {ghIcon}></i>
      <i class= {liIcon}></i>
   </> 
  )
}

export default SocialButton